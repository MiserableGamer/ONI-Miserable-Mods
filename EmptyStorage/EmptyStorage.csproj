<Project Sdk="Microsoft.NET.Sdk">
  <!--
    SDK, publicize, ILRepack, deploy, and version increment are via Directory.Build.props/targets.
  -->

  <PropertyGroup>
    <AssemblyName>EmptyStorage</AssemblyName>
    <RootNamespace>EmptyStorage</RootNamespace>
  </PropertyGroup>

</Project>
