<Project Sdk="Microsoft.NET.Sdk">
  <!-- 
    TEMPLATE: Copy this folder and rename for new mods.
    Only change AssemblyName and RootNamespace below.
    All other settings are inherited from Directory.Build.props/targets.
  -->

  <PropertyGroup>
    <AssemblyName>ControlledCrashes</AssemblyName>
    <RootNamespace>ControlledCrashes</RootNamespace>
  </PropertyGroup>

</Project>
