<Project Sdk="Microsoft.NET.Sdk">
  <!-- 
    ControlledExtraction - Per-building extraction rate control and piped output ports
    SDK-style: NuGet packages, publicizer, ILRepack, deployment all via Directory.Build.props/.targets.
  -->
  <PropertyGroup>
    <AssemblyName>ControlledExtraction</AssemblyName>
    <RootNamespace>ControlledExtraction</RootNamespace>
  </PropertyGroup>

</Project>
